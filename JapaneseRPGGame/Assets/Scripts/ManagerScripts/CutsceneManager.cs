using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CutsceneManager : MonoBehaviour
{
    // put one on each cutscene game object.
    // if its already played before it will fast forward the scene to the end to get the "completed" state of the scene

    // useful for if you have initialised a scene with an animation.
    void Start()
    {
        
    }

    public void MarkCutsceneAsPlayed()
    {
        
    }
}
